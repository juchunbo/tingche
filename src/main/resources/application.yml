server:
  port: 80


spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://10.36.105.247:3306/${MYSQL_DATABASE:springboot_demo}
    username: root
    password: "@Gaoyang12345678"
    hikari:
      max-lifetime: 1800000   # 30分钟
      keepalive-time: 300000 # 5分钟


  mvc:
    view:
      suffix: .html
      prefix: /

mybatis:
  mapper-locations: classpath*:mapper/*Mapper.xml

# 微信云托管配置
# 云托管模式下不再需要 AppSecret、APIv3 Key、商户证书等敏感信息
# 用户身份（openid/unionid）由云托管自动注入到请求 Header
# 支付由云托管开放接口服务 /_/pay/unifiedOrder 统一处理
wechat:
  cloudrun:
    env-id: ${CLOUDRUN_ENV:prod-4gkf9q4ze069b762}  # 云托管环境 ID，用于 unifiedOrder 下单
  mini-program:
    app-id: ${WECHAT_APP_ID:wxeecb2a3bb27d4dbf}
  payment:
    mch-id: ${WECHAT_MCH_ID:1107045416}
