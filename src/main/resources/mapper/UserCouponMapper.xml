<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tencent.wxcloudrun.dao.UserCouponMapper">
    
    <resultMap id="BaseResultMap" type="com.tencent.wxcloudrun.model.UserCoupon">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="user_id" property="userId" jdbcType="BIGINT"/>
        <result column="template_id" property="templateId" jdbcType="BIGINT"/>
        <result column="status" property="status" jdbcType="INTEGER"/>
        <result column="used_at" property="usedAt" jdbcType="TIMESTAMP"/>
        <result column="order_id" property="orderId" jdbcType="BIGINT"/>
        <result column="expires_at" property="expiresAt" jdbcType="TIMESTAMP"/>
        <result column="created_at" property="createdAt" jdbcType="TIMESTAMP"/>
    </resultMap>
    
    <insert id="insert" parameterType="com.tencent.wxcloudrun.model.UserCoupon" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user_coupons (user_id, template_id, status, expires_at)
        VALUES (#{userId}, #{templateId}, #{status}, #{expiresAt})
    </insert>
    
    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT * FROM user_coupons WHERE user_id = #{userId}
        <if test="status != null">
            AND status = #{status}
        </if>
        ORDER BY created_at DESC
    </select>
    
    <select id="selectById" resultMap="BaseResultMap">
        SELECT * FROM user_coupons WHERE id = #{id}
    </select>
    
    <update id="updateStatus">
        UPDATE user_coupons SET status = #{status}
        <if test="orderId != null">
            , order_id = #{orderId}
        </if>
        <if test="status == 2">
            , used_at = NOW()
        </if>
        WHERE id = #{id}
    </update>
    
</mapper>
